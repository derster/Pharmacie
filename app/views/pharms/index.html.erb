<p id="notice"><%= notice %></p>

<h1>Toutes les pharmacies</h1>

<%= link_to 'Ajouter une pharmacie', new_pharm_path, class:'btn btn-success'%>

<table class="table">
  
    <% @pharms.each do |pharm| %>
      <tr>
        <td><%= pharm.commune %></td>
        <td><%= pharm.pharma_name %></td>
        <td>
            <%= link_to 'Show', pharm, class:'btn btn-outline-secondary' %>
            <%= link_to 'Edit', edit_pharm_path(pharm), class:'btn btn-outline-primary' %>
            <%= link_to 'Destroy', pharm, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-outline-danger' %>
        </td>
 
      </tr>

      
      
    <% end %>
 
</table>
 

